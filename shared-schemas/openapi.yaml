openapi: 3.0.0
info:
  title: Aegis Reputation API
  version: 0.1.0
paths:
  /reputation:
    get:
      summary: Query domain reputation
      parameters:
        - in: query
          name: domain
          schema:
            type: string
          required: true
      responses:
        '200':
          description: reputation result
