[build]
rustflags = [
  "-Ccodegen-units=1",
  "-Copt-level=z",
]

[profile.release]
opt-level = "z"
lto = false
codegen-units = 1
debug = false
# Keep default panic strategy to allow tests to run (don't force abort)
