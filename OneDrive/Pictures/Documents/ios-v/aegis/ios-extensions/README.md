ios-extensions — Network Extension integrations

Contains Network Extension implementations and feature-specific folders.

Subfolders

- content-filter: Content filter extension for blocking/allowing requests.
- url-filter: iOS 26+ URL filter implementation path.

Notes

Extensions require entitlements and per-target configuration in Xcode. See Apple docs for Network Extension setup.
