backend-go — Cloud services and reputation APIs

Purpose

Hosts Go services: threat reputation API, rule distribution, telemetry ingestion, admin console backend, and enterprise policy service.

Next steps

- Initialize `go mod` and add a minimal HTTP server scaffold.
- Define OpenAPI/JSON schemas in `shared-schemas`.
