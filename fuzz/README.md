fuzz - Fuzzing harnesses

Purpose

Fuzzers and targets for parsers and engine inputs. Integrate `cargo-fuzz` for Rust, AFL/LibFuzzer for C/C++.

Next steps

- Add example harness and CI integration.
- Seed with corpus from `rules/` and `perf/`.
