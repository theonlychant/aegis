engine-c — C ABI bridge and helpers

Purpose

Small, stable C ABI layer for exposing engine features to Swift and other languages. Include deterministic crypto helpers and minimal platform shims here.

Next steps

- Define FFI-compatible types and safety boundaries.
- Keep implementations tiny; prefer Rust for complex logic.
