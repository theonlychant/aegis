[package]
name = "ffi"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[lib]
name = "ffi"
crate-type = ["staticlib", "cdylib", "rlib"]

[dependencies]
scanner = { path = "../scanner" }
libc = "0.2"

[build-dependencies]
cc = "1.0"
