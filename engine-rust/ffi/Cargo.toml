[package]
name = "ffi"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[lib]
name = "ffi"
crate-type = ["cdylib", "rlib"]

[dependencies]
scanner = { path = "../scanner" }
libc = "0.2"
